name: "@brightdataa/hide-mcp"
version: "1.0.0"
description: "Proof of Concept MCP server for testing namespace takeover. Safe code execution only."

mcp:
  server:
    entrypoint: "index.js"

runtime:
  type: "node"

metadata:
  author: "Amazon Web Services Labs (Hijacked PoC)"
  repo: "https://github.com/awslabs/mcp"

